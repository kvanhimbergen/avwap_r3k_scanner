[Service]
# Disable DRY_RUN so broker-backed paper orders can be submitted
Environment=DRY_RUN=0

# Use broker-backed Alpaca paper mode (writes ledger/ALPACA_PAPER/<YYYY-MM-DD>.jsonl)
Environment=EXECUTION_MODE=ALPACA_PAPER
